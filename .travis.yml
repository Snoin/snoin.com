language: python
python:
- 3.5
install:
- pip install -e .[tests]
- sudo ./powebscr/install_libicu52.sh
- npm install
script:
- py.test tests
- npm run build
after_success:
- python -m powebscr
notifications:
  irc:
    channels:
    - "irc.ozinger.org#snoin-dev"
    on_success: change
    on_failure: always
